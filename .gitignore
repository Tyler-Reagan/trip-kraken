# Dependencies
node_modules/

# Next.js
.next/
out/

# Build
dist/

# Environment — never commit secrets
.env
.env.local
.env.*.local

# Dev server log
dev.log

# Database
*.db
*.db-journal
*.db-wal
*.db-shm

# OSRM road graphs (ADR-0024) — built locally by scripts/build-osrm-graphs.sh from a Geofabrik
# extract; *.db above doesn't match .osrm.* so this needs its own rule.
db/osrm/

# Debug artifacts
debug-screenshot.png
debug-page.html
scripts/tabelog-probe.mjs

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
tsconfig.tsbuildinfo

# impeccable design tooling (critique snapshots, live-mode config — local, not versioned)
.impeccable/
osrm-graphs-20260812.tar.zst

# Claude Code project settings — settings.local.json is machine-local, worktrees/ is scratch
.claude/
